<% @users.each do |user| %>
  <%= user.name %> (<%= user.login %>) currently clocked <%= user.clocked_in? ? "in" : "out" %> at 
  <% if user.last_clock %>
   <%= user.last_clock.localtime.strftime("%x %X")%><br/>
  <% end %>
<% end %>
