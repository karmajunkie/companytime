<% @users.each do |user| %>
  <%= user.name %> (<%= user.login %>) currently clocked <%= user.clocked_in? ? "in" : "out" %> at 
  <% if !user.last_clock.nil? %>
   <%= user.last_clock.localtime.strftime("%x %X") %>
  <% end %><br/>
<% end %>
